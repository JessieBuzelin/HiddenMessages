using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MovementAdvanced : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        // work out which direction the player is pressing W,A,S,D
        // Rotate the character to face that direction 
        //Make them move forward (Which would be in the facing direction
        // transform.forward not Vector3.forward
    }
}
