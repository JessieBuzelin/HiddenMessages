using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NoteCollectionMenu : MonoBehaviour
{ 
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        if (Input.GetKeyDown(KeyCode.Escape))
        {
            // set menu buttons as active
           
        }
    }

    private void OpenNoteCollectionMenu()
    {
        //set note inventory as active 
        //set other menu options as inactive
        //apply to button 
    }

    private void NoteSlot()
    {
        //if click collision open text for note 
    }


}
